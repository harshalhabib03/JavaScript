//how to check given array is empty or not using length property and if else condition

let eids=[]; //101,102,103 //3 //0
let enames=["Raj", "Karan", "sunil", "praveen"]; //0 //4

console.log(eids.length);
console.log(enames.length);

if(eids.length > 0, enames.length>0){
    console.log("Not Empty Array");
}
else{
    console.log("Empty Array");
}